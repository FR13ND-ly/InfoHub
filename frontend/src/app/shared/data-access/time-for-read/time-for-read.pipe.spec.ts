import { TimeForReadPipe } from './time-for-read.pipe';

describe('TimeForReadPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeForReadPipe();
    expect(pipe).toBeTruthy();
  });
});
